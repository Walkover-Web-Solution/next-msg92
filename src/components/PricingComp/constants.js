// Pricing component shared constants

// Scroll and pagination
export const SCROLL_DISTANCE = 752; // Card width (360px) + gap (24px) * 2
export const MIN_PLANS_FOR_SCROLL = 3; // Minimum number of plans before showing scroll arrows

// Table and layout
export const MAX_TABLE_HEIGHT = '600px'; // Maximum height for scrollable tables

// Debounce and timing
export const DEBOUNCE_DELAY = 400; // Milliseconds for search input debounce

// Tab types
export const TAB_TYPES = {
    MONTHLY: 'Monthly',
    YEARLY: 'Yearly',
};

// Searchable fields for dial plan
export const SEARCHABLE_FIELDS = ['country_name', 'prefix'];

// Empty arrays for memoization
export const EMPTY_ARRAY = [];
