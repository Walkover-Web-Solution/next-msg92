import { useState } from 'react';

export default function CalculatePricingSegmento() {
    const [showCalculations, setShowCalculations] = useState(false);

    return (
        <>
            <div className={`modal-box flex flex-col gap-4 `}></div>
        </>
    );
}
